fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
python-multipart==0.0.20

# Testing
pytest==8.3.4
pytest-asyncio==0.25.2

# Phase 3+ (RAG / vector store / agent) dependencies live in requirements-future.txt
# to keep Phase 1 installs fast. Install that file when starting Phase 2+.
