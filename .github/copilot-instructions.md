## Camunda API

Use `backend/docs/camunda-orchestration-openapi.json` as the authoritative
source for all Camunda REST endpoints, request fields, response fields, and
types. Do not infer or guess API field names. If the specification is
ambiguous or incomplete, ask before changing the client.